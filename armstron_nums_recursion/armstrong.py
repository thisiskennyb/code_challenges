def armstrong(num, sum=0):
    sum = sum if sum else 0
    if len(str(num)) == 1:
        return
    else:
        
        sum += str(num)[0]
    
    



# make two separate strings and the sum of their length will be the power to use